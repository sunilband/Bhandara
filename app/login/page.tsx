import React from "react";
import LoginPage from "@/components/LoginPage/LoginPage";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <LoginPage />
    </div>
  );
};

export default page;
